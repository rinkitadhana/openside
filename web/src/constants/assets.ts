// Shared static asset paths (served from /public).

// Dashboard hero / composite image - used by the hero time banner and the
// four quick-action cards. Declared once so the path lives in a single place.
export const DASHBOARD_IMAGE = "/img/dashboard/dashboard.png";
